# 🚨 СРОЧНО: Создайте коллекции для сохранения приемок

## Что сейчас НЕ работает:
- ❌ Кнопка "Готово" не сохраняет приемку
- ❌ Товары не появляются в остатках
- ❌ Нет новых приемок в списке

## Почему НЕ работает:
- ❌ Отсутствуют коллекции `receptions` и `stocks` в PocketBase

## Как ИСПРАВИТЬ (5 минут):

### 1. Откройте админ-панель:
http://192.168.1.4:8090/_/
Логин: admin@example.com
Пароль: admin123456

### 2. Создайте коллекцию "receptions":
1. Нажмите **"New Collection"**
2. Name: `receptions`
3. Нажмите **"Create"**
4. Нажмите **"+ New field"** и добавьте:
   - `supplier` → Relation → suppliers
   - `warehouse` → Relation → warehouses
   - `date` → Date
   - `status` → Select → Values: `draft,done`
   - `items` → JSON
   - `total_amount` → Number
5. Нажмите **"Create"**

### 3. Создайте коллекцию "stocks":
1. Нажмите **"New Collection"**
2. Name: `stocks`
3. Нажмите **"Create"**
4. Нажмите **"+ New field"** и добавьте:
   - `product` → Relation → products
   - `warehouse` → Relation → warehouses
   - `quantity` → Number → Default: `0`
5. Нажмите **"Create"**

### 4. Разрешите доступ:
Для каждой коллекции:
1. Нажмите **"API Rules"**
2. В **"List rule"** выберите **"Public"**
3. Нажмите **"Save changes"**

## ✅ После этого:
- Кнопка "Готово" будет сохранять приемку
- Товары появятся в остатках
- Приемки будут видны в списке

## 🎯 Проверка:
1. Создайте приемку → нажмите "Готово"
2. Перейдите в "Приемка" → увидите новую приемку
3. Перейдите в "Остатки" → увидите товары

**ВСЕ РАБОТАЕТ! Просто создайте коллекции!**
