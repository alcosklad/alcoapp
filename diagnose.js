console.log('🔍 МГНОВЕННАЯ ДИАГНОСТИКА');
console.log('');

// 1. Проверка коллекции users
fetch('http://192.168.1.4:8090/api/collections')
  .then(r => r.json())
  .then(collections => {
    const usersCollection = collections.find(c => c.name === 'users');
    if (usersCollection) {
      console.log('✅ Коллекция users существует');
    } else {
      console.log('❌ Коллекция users НЕ существует');
      console.log('   Создайте её в админ-панели!');
    }
  })
  .catch(e => console.log('❌ Ошибка проверки коллекций:', e.message));

console.log('');
console.log('🚨 ПРОБЛЕМЫ МОГУТ БЫТЬ:');
console.log('1. Обновите страницу (F5) - HMR мог не сработать');
console.log('2. Создайте коллекцию users в админ-панели');
console.log('3. Проверьте API Rules для users = Public');
console.log('4. Откройте консоль (F12) при загрузке страницы');
console.log('');
console.log('🎯 ЕСЛИ НЕ ВИДИТЕ SELECT ПОЛЬЗОВАТЕЛЯ:');
console.log('- Нажмите F5 для полной перезагрузки');
console.log('- Откройте консоль и посмотрите ошибки');
console.log('- Проверьте что коллекция users создана');
