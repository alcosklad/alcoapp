# Руководство по миграции в FIFO систему

## 📋 Что изменилось

### Новая логика учета товаров:
- **Партионный учет** - каждая приемка создает отдельную партию товара
- **FIFO** - товары продаются в порядке поступления (самая старая партия первой)
- **Номера продаж** - уникальные номера по городам (V00001, S00002, K00003...)
- **Себестоимость** - точный расчет себестоимости и прибыли для каждой продажи
- **Списание** - новый раздел для учета испорченных/потерянных товаров

---

## 🚀 ПОРЯДОК ВНЕДРЕНИЯ

### Шаг 1: Обновление схемы базы данных (ОБЯЗАТЕЛЬНО!)

**На сервере через SSH:**
```bash
ssh root@146.103.121.96
```

**Откройте админ-панель PocketBase:**
```
http://146.103.121.96:8090/_/
```

**Выполните все изменения из файла `DB_SCHEMA_UPDATES.md`:**
- Добавьте поля в коллекции `stocks`, `orders`, `receptions`
- Создайте новые коллекции `write_offs`, `favorites`
- Создайте индексы для производительности

**⚠️ БЕЗ ЭТОГО ШАГА ПРИЛОЖЕНИЕ НЕ БУДЕТ РАБОТАТЬ!**

---

### Шаг 2: Деплой нового кода

**Локально (на Mac):**
```bash
cd /Users/rostislavkomkov/Desktop/alcoapp
git add -A
git commit -m "feat: implement FIFO inventory system with batches"
git push origin main
```

**GitHub Actions автоматически задеплоит код на сервер (~2-3 минуты)**

---

### Шаг 3: Миграция существующих данных

**На сервере:**
```bash
ssh root@146.103.121.96
cd /var/www/alcoapp
```

**Тестовый прогон (проверка без изменений):**
```bash
node migrate-to-fifo.mjs --dry-run
```

**Реальная миграция:**
```bash
node migrate-to-fifo.mjs
```

**Что делает миграция:**
1. Преобразует все существующие остатки в партии с `batch_number`
2. Добавляет уникальные номера ко всем продажам (V00001, S00002...)
3. Рассчитывает себестоимость и прибыль для существующих продаж

**Время выполнения:** ~5-10 минут (зависит от объема данных)

---

### Шаг 4: Проверка работоспособности

**Проверьте в приложении:**

1. **Остатки** - должны отображаться партии с номерами
2. **Продажи** - должны иметь уникальные номера (V00001, S00002...)
3. **Приемка** - создайте тестовую приемку, проверьте что создается партия
4. **Продажа** - оформите тестовую продажу, проверьте FIFO логику

---

## 📊 НОВЫЕ ВОЗМОЖНОСТИ

### 1. Просмотр партий товара

**В разделе "Остатки":**
- Кликните на товар → увидите все партии
- Каждая партия показывает: номер, количество, цену закупа, дату приемки
- Кликните на номер партии → откроется детали приемки

### 2. Списание товаров

**В разделе "Остатки":**
- Двойной клик на товар → откроется модалка
- Кнопка "Списать" → выберите партию и причину списания
- Списанные товары попадают в подраздел "Списание"

### 3. Быстрое создание приемки

**В разделе "Приемка":**
- Под поиском появилась панель с категориями
- Выберите товары кнопками +/-
- Укажите цены закупа
- Нажмите "Создать приемку" → все товары добавятся одной приемкой

### 4. Избранное для приемок

**В быстром создании приемки:**
- Галочка рядом с товаром → добавляет в "Избранное"
- Категория "Избранное" показывается первой
- Общее для всех админов

### 5. Детальная статистика продаж

**В разделе "Продажи":**
- Новые колонки: Себестоимость, Прибыль
- Уникальные номера продаж по городам
- Возвраты помечены отдельно

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Коды городов для номеров продаж:
- V = Волгоград
- W = Воронеж
- I = Иркутск
- K = Казань
- L = Калининград
- R = Красноярск
- N = НН
- O = Омск
- P = Пермь
- S = Самара
- T = Саратов
- C = Сочи
- B = СПБ
- U = Сургут
- F = Уфа

### Формат номеров:
- Продажи: `{CODE}{NNNNN}` (например: S00001, V00042)
- Партии: `{CODE}-YYYY-MM-DD-{SEQ}` (например: S-2026-02-16-001)

### FIFO алгоритм:
1. При продаже товара система находит все партии в городе
2. Сортирует по дате приемки (самая старая первая)
3. Берет товар из самой старой партии
4. Если в партии недостаточно - берет из следующей
5. Записывает себестоимость из партии в продажу

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Производительность:
- Партии создают больше записей в БД
- Индексы по `reception_date` и `batch_number` критичны
- FIFO запросы оптимизированы для быстрой работы

### Обратная совместимость:
- Старые продажи без номеров будут отображаться
- Старые остатки преобразуются в партии при миграции
- Все существующие данные сохраняются

### Возвраты:
- Товар возвращается в ту же партию откуда продали
- Если партия была удалена - создается заново
- История возвратов сохраняется

### Списание:
- Товар удаляется из партии
- Создается запись в коллекции `write_offs`
- Отменить списание нельзя (только просмотр)

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка "batch_number field not found"
**Причина:** Не добавлены поля в PocketBase
**Решение:** Выполните Шаг 1 (обновление схемы БД)

### Ошибка "order_number must be unique"
**Причина:** Конфликт номеров при миграции
**Решение:** Запустите миграцию заново, она пропустит уже обработанные записи

### Товар не продается (FIFO ошибка)
**Причина:** Нет партий товара в городе
**Решение:** Проверьте остатки, создайте приемку если нужно

### Медленная работа остатков
**Причина:** Много партий, нет индексов
**Решение:** Создайте индексы в PocketBase (см. DB_SCHEMA_UPDATES.md)

---

## 📞 ПОДДЕРЖКА

При возникновении проблем:
1. Проверьте логи PocketBase: `/var/www/alcoapp/pocketbase.log`
2. Проверьте логи браузера (F12 → Console)
3. Проверьте что все поля добавлены в PocketBase
4. Проверьте что миграция выполнена успешно

---

## ✅ ЧЕКЛИСТ ВНЕДРЕНИЯ

- [ ] Обновлена схема БД в PocketBase (все поля и коллекции)
- [ ] Созданы индексы для производительности
- [ ] Код задеплоен на сервер (git push)
- [ ] Выполнена миграция данных (migrate-to-fifo.mjs)
- [ ] Проверена работа остатков (партии отображаются)
- [ ] Проверена работа продаж (номера генерируются)
- [ ] Протестирована приемка (партии создаются)
- [ ] Протестирована продажа (FIFO работает)
- [ ] Протестировано списание (товары списываются)
- [ ] Проверено быстрое создание приемки
- [ ] Проверено избранное

**Все готово к работе!** 🎉
